/*
 * Altere o módulo de lista de prioridade para que a cada valor real inserido exista
 * associado um objeto genérico, representado por um ponteiro void*. O cliente fica
 * responsável por fornecer na inserção o ponteiro de um objeto já alocado. A função
 * de remoção retorna este objeto associado, não mais o valor da prioridade.
 */
