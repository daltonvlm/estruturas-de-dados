/*
 * Modifique a implementação da função união para que o balanceamento das árvores
 * seja feito em relação às alturas das árvores. Se uma árvores tem altura menor que
 * a outra, a árvore de menor altura passa a ser filha da árvore de maior altura. Note
 * que, neste caso, a altura da árvore resultante não se altera. Se as duas árvores têm
 * a mesma altura, qualquer uma pode ser escolhida para ser filha da outra. Neste
 * caso, a altura da árvore é acrescentada de uma unidade. A altura das árvores deve
 * ser codificada pelo valor absoluto dos números negativos armazenados na raiz. No
 * início, quando cada elementeo está isolado em sua partição, assuma que a altura das
 * árvores é dado por 1 (armazeando o valor -1 na inicialização do vetor).
 */
