/*
 * Estenda a funcionalidade da calculadora pós_fixada, que usa uma pilha de valo-
 * res reais, incluindo novos operadores unários e binários (sugestão: - como menos
 * unário, # como raiz quadrada, ^ como exponenciação).
 */
